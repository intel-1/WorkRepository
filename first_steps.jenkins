#!groovy

properties([disableConcurrentBuilds()])

pipeline {
	agent {
		label 'master'
		}
	options	{		
		buildDiscatded(logRotator(numToKeepStr: '10', artifactNumToKeepStr: '10'))
		timestamps()
	}
	stages {
		stage("My First Stages"){
			steps {
				sh 'ssh root@172.1.0.196 \'hostname\''
			}
		}
	}
}